
const userData = require("./users");
const expenseData = require("./expense")
const budgetData = require("./budget")

module.exports = {
  users: userData,
  expense : expenseData,
  budget : budgetData
};
